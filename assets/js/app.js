(() => {
  const form = document.getElementById('fcm-form');
  if (!form) return;

  const $ = (id) => document.getElementById(id);
  const credentials = $('serviceAccount');
  const fileInput = $('serviceAccountFile');
  const fileState = $('fileState');
  const deviceToken = $('deviceToken');
  const title = $('notificationTitle');
  const message = $('notificationMessage');
  const image = $('notificationImage');
  const data = $('customData');
  const simpleFields = $('simpleFields');
  const advancedFields = $('advancedFields');
  const notificationPreset = $('notificationPreset');
  const presetDescription = $('presetDescription');
  const advancedPayload = $('advancedPayload');
  const payloadPreview = $('payloadPreview');
  const sendButton = $('sendButton');
  const result = $('result');
  const resultTitle = $('resultTitle');
  const resultText = $('resultText');
  const toast = $('toast');

  const TOKEN_ENDPOINT = 'https://oauth2.googleapis.com/token';
  const FCM_SCOPE = 'https://www.googleapis.com/auth/firebase.messaging';

  const NOTIFICATION_PRESETS = {
    '1_basic_notification': {
      description: 'Simple title and message notification.',
      payload: { notification: { title: 'Hello!', body: 'This is a test notification.' } }
    },
    '2_notification_with_data': {
      description: 'Notification with custom data for the application.',
      payload: {
        notification: { title: 'New Message', body: 'John sent you a message.' },
        data: { type: 'chat', chat_id: '12345', sender_id: '98765', screen: 'chat' }
      }
    },
    '3_data_only': {
      description: 'Sends only custom data. No notification UI is automatically generated by FCM.',
      payload: { data: { type: 'sync', action: 'refresh', id: '12345' } }
    },
    '4_notification_with_image': {
      description: 'Notification containing an image.',
      payload: { notification: { title: 'Special Offer', body: 'Get 30% off today!', image: 'https://example.com/images/offer.jpg' } }
    },
    '5_android_notification': {
      description: 'Android-specific notification settings.',
      payload: {
        notification: { title: 'Android Notification', body: 'This notification has Android-specific settings.' },
        android: { priority: 'high', notification: { channel_id: 'default', sound: 'default' } }
      }
    },
    '6_android_notification_with_image': {
      description: 'Android notification with an image.',
      payload: {
        notification: { title: 'New Product', body: 'Check out our latest product.' },
        android: { priority: 'high', notification: { channel_id: 'default', sound: 'default', image: 'https://example.com/images/product.jpg' } }
      }
    },
    '7_ios_notification': {
      description: 'iOS/APNs-specific notification settings.',
      payload: {
        notification: { title: 'iOS Notification', body: 'This notification has iOS-specific settings.' },
        apns: { headers: { 'apns-priority': '10' }, payload: { aps: { sound: 'default', badge: 1 } } }
      }
    },
    '8_ios_notification_with_image': {
      description: 'iOS notification with rich media/image support.',
      payload: {
        notification: { title: 'New Offer', body: 'Check out this special offer.' },
        apns: {
          headers: { 'apns-priority': '10' },
          payload: { aps: { sound: 'default', badge: 1, 'mutable-content': 1 } },
          fcm_options: { image: 'https://example.com/images/offer.jpg' }
        }
      }
    },
    '9_android_ios': {
      description: 'Cross-platform notification with platform-specific configuration.',
      payload: {
        notification: { title: 'New Message', body: 'You have received a new message.' },
        android: { priority: 'high', notification: { channel_id: 'default', sound: 'default' } },
        apns: { headers: { 'apns-priority': '10' }, payload: { aps: { sound: 'default', badge: 1 } } }
      }
    },
    '10_android_ios_with_image': {
      description: 'Cross-platform rich notification with an image.',
      payload: {
        notification: { title: 'Special Offer', body: 'Get 30% off your next order.', image: 'https://example.com/images/offer.jpg' },
        android: { priority: 'high', notification: { channel_id: 'default', sound: 'default', image: 'https://example.com/images/offer.jpg' } },
        apns: {
          headers: { 'apns-priority': '10' },
          payload: { aps: { sound: 'default', badge: 1, 'mutable-content': 1 } },
          fcm_options: { image: 'https://example.com/images/offer.jpg' }
        }
      }
    },
    '11_full_notification': {
      description: 'Notification with title, message, image, data, Android configuration and iOS configuration.',
      payload: {
        notification: { title: 'New Product Available', body: 'Check out our latest product.', image: 'https://example.com/images/product.jpg' },
        data: { type: 'product', product_id: '12345', screen: 'product_details', action: 'open_product' },
        android: { priority: 'high', notification: { channel_id: 'default', sound: 'default', image: 'https://example.com/images/product.jpg' } },
        apns: {
          headers: { 'apns-priority': '10' },
          payload: { aps: { sound: 'default', badge: 1, 'mutable-content': 1 } },
          fcm_options: { image: 'https://example.com/images/product.jpg' }
        }
      }
    }
  };

  function base64url(bytes) {
    let binary = '';
    for (let i = 0; i < bytes.length; i++) binary += String.fromCharCode(bytes[i]);
    return btoa(binary).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  }

  function base64urlFromString(text) {
    return base64url(new TextEncoder().encode(text));
  }

  async function importPrivateKey(pem) {
    const base64 = pem.replace(/-----BEGIN PRIVATE KEY-----/, '').replace(/-----END PRIVATE KEY-----/, '').replace(/\s+/g, '');
    const binary = atob(base64);
    const bytes = new Uint8Array(binary.length);
    for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
    try {
      return await crypto.subtle.importKey('pkcs8', bytes.buffer, { name: 'RSASSA-PKCS1-v1_5', hash: 'SHA-256' }, false, ['sign']);
    } catch {
      throw new Error('The service account private_key could not be read. Re-download the JSON from Firebase.');
    }
  }

  // Signs a JWT and exchanges it for an FCM access token entirely in the browser,
  // so the private key never leaves this page.
  async function mintAccessToken(serviceAccount) {
    const now = Math.floor(Date.now() / 1000);
    const header = base64urlFromString(JSON.stringify({ alg: 'RS256', typ: 'JWT' }));
    const claim = base64urlFromString(JSON.stringify({
      iss: serviceAccount.client_email,
      scope: FCM_SCOPE,
      aud: TOKEN_ENDPOINT,
      iat: now,
      exp: now + 3600
    }));
    const unsigned = `${header}.${claim}`;
    const key = await importPrivateKey(serviceAccount.private_key);
    const signature = await crypto.subtle.sign('RSASSA-PKCS1-v1_5', key, new TextEncoder().encode(unsigned));
    const jwt = `${unsigned}.${base64url(new Uint8Array(signature))}`;

    const response = await fetch(TOKEN_ENDPOINT, {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: new URLSearchParams({ grant_type: 'urn:ietf:params:oauth:grant-type:jwt-bearer', assertion: jwt })
    });
    const payload = await response.json().catch(() => null);
    if (!response.ok || !payload?.access_token) {
      throw new Error(payload?.error_description || payload?.error || 'Could not authenticate the Firebase service account.');
    }
    return payload.access_token;
  }

  function firebaseErrorMessage(payload, status) {
    const code = payload?.error?.status || '';
    const message = payload?.error?.message || '';
    if (code === 'UNAUTHENTICATED') return 'Firebase could not authenticate this service account. Check the JSON key and its permissions.';
    if (code === 'PERMISSION_DENIED') return 'This service account is not allowed to send FCM messages for this Firebase project.';
    if (code === 'NOT_FOUND') return 'Firebase could not find the requested project or registration.';
    if (code === 'INVALID_ARGUMENT') return message || 'Firebase rejected the device token or message payload.';
    return message || `Firebase returned HTTP ${status}.`;
  }

  async function sendFcmMessage(serviceAccount, message) {
    const accessToken = await mintAccessToken(serviceAccount);
    const url = `https://fcm.googleapis.com/v1/projects/${encodeURIComponent(serviceAccount.project_id)}/messages:send`;
    const response = await fetch(url, {
      method: 'POST',
      headers: { Authorization: `Bearer ${accessToken}`, 'Content-Type': 'application/json' },
      body: JSON.stringify({ message })
    });
    const payload = await response.json().catch(() => ({}));
    if (!response.ok) {
      const error = new Error(firebaseErrorMessage(payload, response.status));
      error.rejectedByFirebase = true;
      throw error;
    }
    return payload;
  }

  function toastMessage(text, type = '') {
    if (!toast) return;
    toast.textContent = text;
    toast.className = `toast show ${type}`.trim();
    clearTimeout(toastMessage.timer);
    toastMessage.timer = setTimeout(() => { toast.className = 'toast'; }, 2800);
  }

  function parseServiceAccount() {
    const raw = credentials.value.trim();
    if (!raw) throw new Error('Add your Firebase service account JSON.');
    let parsed;
    try { parsed = JSON.parse(raw); } catch { throw new Error('The service account JSON is not valid JSON.'); }
    if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('The service account JSON must be an object.');
    const required = ['project_id', 'client_email', 'private_key'];
    const missing = required.filter((key) => !parsed[key] || typeof parsed[key] !== 'string');
    if (missing.length) throw new Error(`Service account JSON is missing: ${missing.join(', ')}.`);
    if (!parsed.client_email.includes('@')) throw new Error('The service account client_email looks invalid.');
    if (!parsed.private_key.includes('BEGIN PRIVATE KEY') || !parsed.private_key.includes('END PRIVATE KEY')) {
      throw new Error('The service account private_key looks invalid.');
    }
    return parsed;
  }

  function parseCustomData() {
    const raw = data.value.trim();
    if (!raw) return undefined;
    let parsed;
    try { parsed = JSON.parse(raw); } catch { throw new Error('Custom data must be valid JSON.'); }
    if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('Custom data must be a JSON object.');
    const output = {};
    for (const [key, value] of Object.entries(parsed)) {
      if (!key.trim()) throw new Error('Custom data keys cannot be empty.');
      output[key] = typeof value === 'string' ? value : JSON.stringify(value);
    }
    return output;
  }

  function parseUrlField(el, label) {
    const raw = el.value.trim();
    if (!raw) return undefined;
    try {
      const parsed = new URL(raw);
      if (parsed.protocol !== 'http:' && parsed.protocol !== 'https:') throw new Error();
    } catch {
      throw new Error(`${label} must be a valid http(s) URL.`);
    }
    return raw;
  }

  function parseAdvancedPayload() {
    const raw = advancedPayload.value.trim();
    if (!raw) throw new Error('Paste a message payload, or pick a template above.');
    let parsed;
    try { parsed = JSON.parse(raw); } catch { throw new Error('The payload must be valid JSON.'); }
    if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('The payload must be a JSON object.');
    return parsed;
  }

  function getMode() {
    return form.querySelector('input[name="notifMode"]:checked')?.value || 'simple';
  }

  function buildSimpleMessage(token, notificationTitle, notificationMessage, imageUrl, customData) {
    const fcmMessage = { token };
    if (notificationTitle || notificationMessage || imageUrl) {
      fcmMessage.notification = {};
      if (notificationTitle) fcmMessage.notification.title = notificationTitle;
      if (notificationMessage) fcmMessage.notification.body = notificationMessage;
      if (imageUrl) fcmMessage.notification.image = imageUrl;
    }
    if (customData) fcmMessage.data = customData;
    return fcmMessage;
  }

  function buildAdvancedMessage(token, payload) {
    const { token: _ignored, ...rest } = payload;
    return { token, ...rest };
  }

  function refreshPayloadPreview() {
    if (!payloadPreview) return;
    const token = deviceToken.value.trim() || '<your-device-token>';
    let fcmMessage;
    if (getMode() === 'advanced') {
      let payload;
      try { payload = parseAdvancedPayload(); } catch { payload = {}; }
      fcmMessage = buildAdvancedMessage(token, payload);
    } else {
      let previewData;
      try { previewData = parseCustomData(); } catch { previewData = undefined; }
      let imageUrl;
      try { imageUrl = parseUrlField(image, 'Image URL'); } catch { imageUrl = image.value.trim(); }
      fcmMessage = buildSimpleMessage(token, title.value.trim(), message.value.trim(), imageUrl, previewData);
    }
    payloadPreview.textContent = JSON.stringify({ message: fcmMessage }, null, 2);
  }

  function showResult(ok, heading, detail) {
    result.hidden = false;
    result.className = `result ${ok ? 'success' : 'error'}`;
    resultTitle.textContent = heading;
    resultText.textContent = detail;
    result.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }

  fileInput.addEventListener('change', async () => {
    const file = fileInput.files?.[0];
    if (!file) return;
    if (!file.name.toLowerCase().endsWith('.json')) {
      fileInput.value = '';
      toastMessage('Choose a .json service account file.', 'error');
      return;
    }
    if (file.size > 128 * 1024) {
      fileInput.value = '';
      toastMessage('The JSON file is unexpectedly large.', 'error');
      return;
    }
    try {
      const text = await file.text();
      JSON.parse(text);
      credentials.value = text;
      fileState.textContent = file.name;
      fileState.classList.add('loaded');
      toastMessage('Service account JSON loaded.', 'success');
    } catch {
      fileInput.value = '';
      fileState.textContent = 'No file selected';
      fileState.classList.remove('loaded');
      toastMessage('That file is not valid JSON.', 'error');
    }
  });

  credentials.addEventListener('input', () => {
    if (!credentials.value.trim()) {
      fileInput.value = '';
      fileState.textContent = 'No file selected';
      fileState.classList.remove('loaded');
    }
  });

  const modeRadios = Array.from(form.querySelectorAll('input[name="notifMode"]'));
  modeRadios.forEach((radio) => {
    radio.addEventListener('change', () => {
      modeRadios.forEach((r) => r.closest('.mode-option').classList.toggle('is-active', r.checked));
      const advanced = getMode() === 'advanced';
      simpleFields.hidden = advanced;
      advancedFields.hidden = !advanced;
      refreshPayloadPreview();
    });
  });

  notificationPreset.addEventListener('change', () => {
    const preset = NOTIFICATION_PRESETS[notificationPreset.value];
    if (!preset) return;
    advancedPayload.value = JSON.stringify(preset.payload, null, 2);
    presetDescription.textContent = preset.description;
    refreshPayloadPreview();
  });

  [deviceToken, title, message, image, data, advancedPayload].forEach((el) => el.addEventListener('input', refreshPayloadPreview));
  refreshPayloadPreview();

  form.addEventListener('submit', async (event) => {
    event.preventDefault();
    result.hidden = true;

    let serviceAccount;
    try {
      serviceAccount = parseServiceAccount();
    } catch (error) {
      toastMessage(error.message, 'error');
      return;
    }

    const token = deviceToken.value.trim();
    if (!token) return toastMessage('Enter the device token.', 'error');

    const mode = getMode();
    let fcmMessage;

    if (mode === 'advanced') {
      let payload;
      try {
        payload = parseAdvancedPayload();
      } catch (error) {
        toastMessage(error.message, 'error');
        return;
      }
      fcmMessage = buildAdvancedMessage(token, payload);
    } else {
      let customData;
      let imageUrl;
      try {
        customData = parseCustomData();
        imageUrl = parseUrlField(image, 'Image URL');
      } catch (error) {
        toastMessage(error.message, 'error');
        return;
      }
      const notificationTitle = title.value.trim();
      const notificationMessage = message.value.trim();
      if (!notificationTitle && !notificationMessage && !imageUrl && !customData) {
        return toastMessage('Add a title, message, image, or custom data.', 'error');
      }
      fcmMessage = buildSimpleMessage(token, notificationTitle, notificationMessage, imageUrl, customData);
    }

    sendButton.disabled = true;
    sendButton.classList.add('is-sending');
    const buttonLabel = sendButton.querySelector('.button-label');
    if (buttonLabel) buttonLabel.textContent = 'Sending...';

    try {
      const payload = await sendFcmMessage(serviceAccount, fcmMessage);
      const id = payload?.name || 'Accepted by Firebase Cloud Messaging.';
      showResult(true, 'Notification sent', id);
      toastMessage('Notification sent successfully.', 'success');
    } catch (error) {
      if (error.rejectedByFirebase) {
        showResult(false, 'Notification not sent', error.message);
        toastMessage('Notification failed.', 'error');
      } else {
        showResult(false, 'Unable to send notification', error.message || 'Check your connection and try again.');
        toastMessage('Unable to reach Firebase.', 'error');
      }
    } finally {
      sendButton.disabled = false;
      sendButton.classList.remove('is-sending');
      const buttonLabel = sendButton.querySelector('.button-label');
      if (buttonLabel) buttonLabel.textContent = 'Send Notification';
    }
  });
})();
